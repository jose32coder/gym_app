import 'package:flutter/material.dart';

class AdvancedSecurity extends StatelessWidget {
  const AdvancedSecurity({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Próximamente...',
        style: TextStyle(
          fontSize: 24,
          fontWeight: FontWeight.bold,
        ),
      ),
    );
  }
}
